# Copiar como .env y ajustar antes de levantar docker-compose
# Estas variables solo controlan la base de datos interna de Odoo (Postgres),
# NO la conexión hacia ArcGIS (esa vive en integration-api/.env)

ODOO_DB_USER=odoo
ODOO_DB_PASSWORD=change_me_odoo_pg_password
