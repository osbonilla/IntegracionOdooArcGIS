[options]
; Config mínima para demo local. NO usar tal cual en producción.
; Cambiar admin_passwd por un valor no trivial antes de crear la base de
; datos, y no reutilizarlo como password de ningún usuario de la app.
admin_passwd = change_me_admin_password
dbfilter = ^odoo_demo$
list_db = True
proxy_mode = False
